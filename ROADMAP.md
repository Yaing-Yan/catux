# CatUX 路线图

> 目标：从零写一个 Unix-like 内核，最终打包成自己的发行版（可引导 ISO）。
> 架构：x86 32 位起步（资料最多、坑最少、概念最完整），后期再升 64 位。
> 工具链：clang + GNU ld + QEMU（系统 gcc 损坏，clang 内置 32 位代码生成）。

## 我们的协作方式

- 每一步：讲原理 → 写带详细注释的代码 → 一起跑通验证 → 你动手做小练习
- 任何一行看不懂就问；出 bug 把现象/报错贴给我，一起修
- 永远保持"能启动"状态，每一步都有成果可以炫耀

## 阶段 0：跑起来 ✅（当前）

- [x] 工具链检查（clang / ld / QEMU / make）
- [x] Multiboot 头 + 引导汇编 + C 入口
- [x] VGA 文本输出 + 串口输出
- 知识点：BIOS→引导程序→内核的开机流程、ELF 与链接脚本、端口 I/O、栈

## 阶段 1：让内核能说话 ✅

- [x] VGA 终端驱动：光标、换行、滚动、清屏、颜色 API
- [x] 迷你 printf（%d %x %s %c）
- 知识点：显存布局、用 C 指针直接操作硬件、格式化输出的实现

## 阶段 2：听懂硬件（中断）✅

- [x] IDT（中断描述符表）+ 异常处理（除零会打红色警报）
- [x] PIC 重映射（IRQ0~15 → 向量 32~47）+ EOI 应答
- [x] PIT 时钟中断（100Hz 心跳，每秒报 uptime）
- [x] 键盘驱动：敲键盘，字符上屏
- 注：GDT 暂用引导程序提供的，等阶段 6 进用户态时再自建（含 TSS）
- 知识点：CPU 如何响应硬件事件、保存/恢复现场（pusha/iretd）、EOI

## 阶段 3：管理内存

- [ ] 从 Multiboot 信息里读物理内存布局
- [ ] 物理页帧分配器（bitmap）+ 开启分页（虚拟内存！）
- [ ] kmalloc/kfree 堆分配器
- 知识点：虚拟地址 vs 物理地址、页表、为什么 malloc 不是魔法

## 阶段 4：多个任务（进程雏形）

- [ ] 时钟中断驱动的上下文切换
- [ ] 内核线程 + 简单调度器（round-robin）
- 知识点：寄存器保存/恢复、栈切换、并发初体验

## 阶段 5：读磁盘、有文件（进行中）

- [x] 内存盘 initrd：引导程序把 tar 归档塞进内存，内核解析出文件
- [x] 迷你文件系统（tar-as-FS）+ shell 的 ls / cat
- [ ] ATA PIO 磁盘驱动（真磁盘读写，替换 initrd 后端）
- [ ] ext2-lite：从 tar 升级到真正的磁盘文件系统（fs.h 接口不变）
- 知识点：块设备、目录项、宿主机与内核协作打包

## 阶段 6：用户态（质的飞跃）

- [ ] 系统调用 int 0x80（read/write/open/…）
- [ ] ring3 用户模式 + ELF 程序加载器
- [ ] 内核启动的第一个用户程序
- 知识点：内核态/用户态隔离、syscall 协议

## 阶段 7：Unix 化

- [ ] fork / exec / wait 进程模型
- [ ] 管道、信号
- [ ] 极简 libc（crt0 + 系统调用封装）
- [ ] 交互式 shell
- 知识点：Unix 设计哲学的核心——"一切皆文件"、进程即树

## 阶段 8：发行版！

- [ ] 用户态工具集（ls cat echo ps …）、init 进程
- [ ] GRUB 引导的正式 ISO + 安装脚本
- 知识点：发行版 = 内核 + 用户空间 + 引导 + 打包

## 长期可选

64 位升级（long mode）、SMP 多核、网络栈（TCP/IP）、USB。

## GUI 里程碑（shell 之后）：TrueType 字体渲染

目标：用 MartianMono Nerd Font（SIL OFL 许可，可自由捆绑发行）逐像素渲染文字。
需要三块积木：
1. VBE 图形模式：引导期切分辨率，拿到线性帧缓冲（VGA 文本模式退役）
2. 帧缓冲控制台：替代 console.c，支持任意像素绘制
3. TTF 光栅化：移植公有领域的 stb_truetype.h——解析字形轮廓 → 贝塞尔
   曲线采样 → 栅格化 → 抗锯齿灰度位图 → 按 Unicode 码点查找绘制

## 环境备注

- 缺 `xorriso` 和 `mtools`（做 ISO 时才需要）：
  `sudo pacman -S --needed xorriso mtools`
- 系统 gcc 的 cc1 损坏，Makefile 已改用 clang，不影响任何事
